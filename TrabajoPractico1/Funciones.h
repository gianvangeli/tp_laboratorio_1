#include <stdio.h>
#include <stdlib.h>

/** \brief obtiene dos numeros de una funcion y realiza la suma.
 *
 * \param primer numero (A) float sumando
 * \param segundo numero (B) float sumando
 * \return float retorna el resultado de la suma
 *
 */
float sumarOperandos (float x, float y);
/** \brief obtiene dos numeros de una funcion y realiza la resta
 *
 * \param primer numero (A) float restando
 * \param segundo numero (B) float restando
 * \return float retorna el resultado de la resta
 *
 */


float restarOperandos (float x, float y);
/** \brief obtiene dos numeros de una funcion y realiza la division
 *
 * \param primer numero (A) float dividiendo
 * \param segundo numero (B) float dividiendo
 * \return float retorna el resultado de la division
 *
 */

float dividirOperandos (float x, float y);
/** \brief obtiene dos numeros de una funcion y realiza la multiplicacion
 *
 * \param primer numero (A) floatante multiplicando
 * \param segundo numero (B) floatante multiplicando
 * \return float retorna el resultado  de la multiplicacion
 *
 */

float multiplicarOperandos (float x, float y);
/** \brief obtiene un numero de una funcion y calcula el factorial
 *
 * \param primer numero (A) int factorial
 * \return retorna un numero entero y muestra el resultado
 *
 */
int calcularFactorial(int x);

